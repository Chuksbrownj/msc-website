import { describe, it, expect } from "vitest";

describe("MSC Website", () => {
  it("should have correct project name", () => {
    const name = "msc-website";
    expect(name).toBe("msc-website");
  });

  it("should have a placeholder test", () => {
    expect(true).toBe(true);
  });
});
